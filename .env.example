# 后端 API 地址（Expo 要求使用 EXPO_PUBLIC_ 前缀才能在客户端读取）
# 不设置时使用 constants/config.ts 中的默认值（localhost，适合本机/模拟器）

# ── 后端在 Docker 时 ─────────────────────────────────────
# 模拟器 / 本机 Expo：Docker 端口映射到本机后可直接用 localhost（默认已用）
# EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1

# 真机调试：必须和 Metro 同一台电脑的 IP（Expo 启动时显示的 url 里的 IP，如 192.168.1.6）
# EXPO_PUBLIC_API_URL=http://192.168.1.6:8080/api/v1

# Android 模拟器访问宿主机（可选）
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8080/api/v1

# ── 测试/正式环境 ────────────────────────────────────────
# EXPO_PUBLIC_API_URL=https://api.example.com/api/v1
